apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "afc-int.fullname" . }}-test-connection"
  labels:
    {{- include "afc-int.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "afc-int.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
