apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "afc-ext.fullname" . }}-test-connection"
  labels:
    {{- include "afc-ext.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "afc-ext.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
