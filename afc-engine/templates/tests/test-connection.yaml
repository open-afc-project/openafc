apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "afc-engine.fullname" . }}-test-connection"
  labels:
    {{- include "afc-engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "afc-engine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
